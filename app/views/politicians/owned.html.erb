<div class="container">


  <h1 class="text-center">My Politicians 🤝💸</h1>
  <div class="cards d-flex flex-wrap">

    <% @politicians.each do |poli| %>
    <%= link_to politician_path(poli.id) do %>
      <%= render partial: 'politician', locals: { politician: poli } %>
    <% end %>
  <% end %>

  </div>

</div>
