  <div class="card-product border">
    <div class="image_rooms" style="background-image: asset-url('Room1.jpeg')">
    <% if politician.image_url.present? %>
            <img src="<%= politician.image_url %>" alt="<%= politician.name %>" width="100%" height="100%">
          <% else %>
          <%= cl_image_tag(politician.photo.key, width: 50, height: 60, crop: :fill) %>
          <% end %>
    </div>
    <div class="card-product-infos ">
      <h2>Poli-name: <%= politician.name %></h2>
      <hr class="solid">
      <p class="mt-3 mb-3">Description: <%= politician.description[0..100] %></p>
      <div class="row">
      <br>
      <hr class="solid">
      <h6 class="col-6" style="color: #816F5D">Location: <%= politician.location %></h6>
      <h6 class="col-6"><%= politician.cost %> €/night</h6>
      </div>
    </div>
  </div>
